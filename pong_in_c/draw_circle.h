#include <SDL3/SDL.h>

#ifndef SDL_RenderDrawCircle
void SDL_RenderDrawCircle(SDL_Renderer *renderer, int x, int y, int radius);
void SDL_RenderFillCircle(SDL_Renderer *renderer, int x, int y, int radius);

#endif
